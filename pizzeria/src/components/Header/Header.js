import React from "react";

import classes from "./Header.module.css";

const header = () => (
  <header className={classes.Header}>
    <h1>Pizzeria Dante</h1>
  </header>
);

export default header;
